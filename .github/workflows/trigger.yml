name: Trigger

on:
  push:
    branches:
      - main

jobs:
  trigger_ci_cd:
    runs-on: self-hosted
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Run CI/CD workflows
      uses: ./.github/workflows

